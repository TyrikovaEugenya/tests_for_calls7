В этом файле приведена документация репозитория, разбитая по файлам.

## config.py
### **Назначение файла:**

Файл содержит все конфигурационные параметры для фреймворка автоматизированного тестирования, включая URL, селекторы, пороговые значения метрик и формулы расчетов.

### **Основные разделы:**

#### 1. **URL Configuration**

- `BASE_URL` - основной URL тестируемого приложения
    
- `CHROMIUM_PATH` - путь к браузеру Chromium (для отладки)
    
#### 2. **Selectors Configuration**

Словарь CSS-селекторов для элементов приложения:

- Элементы видео и навигации
    
- Попапы и платежные формы
    
- Кнопки оплаты разных методов
    

#### 3. **Performance Metrics**

- `TARGET_PAGE_PERFORMANCE_INDEX` - целевой показатель производительности
    
- `METRIC_WEIGHTS` - веса метрик для расчета общего индекса
    
- `METRIC_THRESHOLDS` - пороговые значения для оценки метрик
    

#### 4. **Test Parameters**

Параметры для параметризации тестов:

- Устройства, режимы сети, геолокации
    
- Браузеры и методы оплаты
    

### **Ключевые функции:**

#### `grade_metric(value, metric_name)`

Оценивает метрику по пороговым значениям и возвращает текстовую оценку.

#### `calculate_page_performance_index(...)`

Рассчитывает комплексный индекс производительности страницы на основе метрик Web Vitals.

## conftest.py
Файл обеспечивает:
- **Управление параметрами тестирования** через командную строку
    
- **Инициализацию и настройку браузера** с помощью Playwright
    
- **Динамическую параметризацию** тестовых сценариев
    
- **Сбор и агрегацию метрик** производительности
    
- **Интеграцию с системой отчетности** Allure
    
- **Обработку ошибок** и создание артефактов

### Основные компоненты
#### 1. Конфигурация геолокаций
```python
geo_map: Dict[str, Tuple[str, str]] = {
    "Moscow": ("ru-RU", "Europe/Moscow"),
    "SPb": ("ru-RU", "Europe/Moscow"),
    "Kazan": ("ru-RU", "Europe/Moscow"),
    "Novosibirsk": ("ru-RU", "Asia/Novosibirsk"),
    "Yekaterinburg": ("ru-RU", "Asia/Yekaterinburg"),
}
```
**Параметры**:
- **Ключ**: название города (используется в CLI)
    
- **Значение**: кортеж (локаль, часовой пояс)

**Использование**:
- Локаль (`ru-RU`) влияет на язык интерфейса и форматирование
    
- Часовой пояс важен для временных метрик и отображения расписаний

#### 2. Пользовательские опции командной строки

Функция `pytest_addoption` добавляет кастомные параметры для гибкого управления тестированием:

| Опция          | Тип    | По умолчанию  | Описание                             |
| -------------- | ------ | ------------- | ------------------------------------ |
| `--film-url`   | string | демо-фильм    | Одиночный URL для тестирования       |
| `--film-list`  | path   | None          | Путь к файлу со списком URL          |
| `--film-limit` | int    | None          | Ограничение количества URL из списка |
| `--device`     | choice | Desktop       | Тип устройства (Desktop/Mobile)      |
| `--throttling` | choice | No_throttling | Режим эмуляции сети                  |
| `--geo`        | choice | Moscow        | Географическая локация               |
| `--browser`    | choice | chromium      | Браузер для тестирования             |
| `--pay-method` | choice | card          | Метод оплаты                         |

#### 3. Фикстуры параметров тестирования
Каждая опция командной строки представлена соответствующей фикстурой. Особенности:
- `scope="session"` для браузера - переиспользуется в течение всей сессии
- `scope="function"` для остальных параметров - пересоздается для каждого теста

#### 4. Утилиты для работы с файлами

#### `load_film_urls(film_list_path: str, limit: Optional[int] = None) -> List[str]`

**Функциональность**:
- Загрузка URL из различных форматов файлов
- Поддержка JSON и TXT форматов
- Ограничение количества через параметр `limit`

Поддерживаемые форматы:
```json
{
    "urls": [
        "https://example.com/film/1",
        "https://example.com/film/2"
    ]
}
```

```text
https://example.com/film/1
https://example.com/film/2
```

**Обработка ошибок**:
- `FileNotFoundError` - если файл не существует
- `ValueError` - если формат файла не поддерживается

#### 5. Динамическая параметризация тестов
1. **Автоматическое определение режима**:
    
    - Если переданы CLI опции - используется режим одиночного тестирования
        
    - Если опции не переданы - включается полная параметризация
        
2. **Гибкая обработка URL**:
    
    - Приоритет у `--film-list` над `--film-url`
        
    - Автоматическая генерация человеко-читаемых ID для параметризованных тестов
        
    - Обработка случая отсутствия входных данных

#### 6. Управление браузером и страницами
#### `playwright_instance`

- **Scope**: session
    
- **Назначение**: Единый экземпляр Playwright для всей сессии тестирования
    
- **Жизненный цикл**: Создается при старте сессии, закрывается при завершении
    

#### `browser_instance`

- **Scope**: session
    
- **Назначение**: Запущенный браузер указанного типа
    
- **Особенности для Chromium**:
    
    - Использование системного Chromium (`CHROMIUM_PATH`)
        
    - Аргументы для стабильности в CI/CD окружениях
        
- **Особенности для Firefox/WebKit**: Стандартный запуск
    

#### `page`
- **Scope**: function
- **Назначение**: Страница с указанными параметрами
- **Настройка контекста**:
Устройство:
```python
	if device == "Mobile":
	    context_args = p.devices["Pixel 5"]  # Эмуляция мобильного устройства
	else:
	    context_args = {"viewport": {"width": 1920, "height": 1080}}  # Десктоп
```
Геолокация:
```python
locale, timezone = geo_map.get(geo, ("ru-RU", "UTC"))
context_args.update({
    "locale": locale,        # Язык и региональные настройки
    "timezone_id": timezone, # Часовой пояс
})
```
Общие настройки:
- User Agent для эмуляции реального браузера
- Разрешения (геолокация, уведомления)
- Включенный JavaScript
- Защита от обнаружения:
```javascript
// Скрытие navigator.webdriver для обхода защиты
Object.defineProperty(navigator, 'webdriver', {
    get: () => undefined
});
```
Мониторинг консоли:
- Перехват всех console.* методов
- Сохранение сообщений в `window.__consoleMessages`
- Детектирование готовности видеоплеера

Настройки для Chromium:
- CDP (Chrome DevTools Protocol) сессия
- Мониторинг логов браузера
- Применение троттлинга сети через Network.emulateNetworkConditions

Троттлинг Slow_4G:
```python
{
    "offline": False,
    "latency": 400,           # 400ms задержка
    "downloadThroughput": 700 * 1024,  # ~700 Kbps
    "uploadThroughput": 700 * 1024,    # ~700 Kbps  
    "connectionType": "cellular4g"
}
```

#### 7. Обработка результатов тестов
Хук `pytest_runtest_makereport`

1. **Создание скриншотов при падении**:
    - Автоматический скриншот страницы при любом падении теста
    - Прикрепление к Allure отчетам
        
2. **Сбор метрик в агрегатор**:
    - Сохранение данных даже для упавших тестов
    - Передача в `MultiTestRunAggregator` для последующего анализа

#### 8. Управление тестовой сессией
**Хуки жизненного цикла:**

**`pytest_collection_finish`**:

- Подсчет общего количества параметризованных тестов
- Определение сколько раз должен выполниться каждый тест

**`pytest_sessionstart`**:

- Фиксация времени начала тестовой сессии

**`pytest_sessionfinish`**:

- Генерация финальных отчетов
- Создание `environment.properties` для Allure

**`pytest_runtest_logfinish`**:

- Отслеживание завершения каждого параметризованного запуска
- Автоматическое сохранение агрегированных отчетов при завершении всех запусков теста

#### 9. Генерация отчетов для Allure (главной страницы)
Функция `aggregate_reports()`
Собирает ключевые метрики по последней сессии тестов.
```python
env = {
    "Start time": "2024-01-01 10:00:00",
    "End time": "2024-01-01 11:30:00", 
    "Duration": "5400.0 sec",
    "Pages": "150 / 19975",
    "Problematic pages": "15 (10.0%)",
    "Failed by errors": "5 (3.3%)",
    "Quality score": "90%",
    "film_page.videoStartTime > 15 sec": "8 (5.3%)",
    "main_page.LCP > 2500 ms": "12 (8.0%)", 
    "pay_page.iframeCpLoadTime > 3 sec": "3 (2.0%)"
}
```

#### 10. Интеграция с Telegram
Набросок функции, отправляющей уведомление о завершении тестирования.
**Параметры**:
- `summary_text`: текст сводки
- `chat_id`: ID чата для отправки
- `bot_token`: токен Telegram бота

## aggregator.py
### MultiTestRunAggregator
Класс предназначен для:
- **Агрегации результатов** множественных запусков одного теста.
- **Статистического анализа** метрик производительности.
- **Кластеризации данных** по различным параметрам (устройство, геолокация, браузер и т.д.)
- **Генерации отчетов** в форматах JSON и Markdown.
- **Выявления проблемных сценариев** и аномалий.

#### Основные компоненты
##### Основная агрегация данных
**Метод `get_summary()`** выполняет комплексный анализ:

- Собирает статистику по всем запускам.
- Агрегирует метрики по шагам теста.
- Анализирует распределение параметров.
- Выявляет ошибки и проблемные сценарии.

**Ключевые метрики агрегации:**

- **Числовые метрики**: среднее, медиана, стандартное отклонение.
- **Булевы метрики**: процент успешных выполнений.
- **PPI (Page Performance Index)**: сводный показатель производительности.

##### Генерация отчетов
**Метод `save_summary()`** создает:

- **JSON отчет** для машинной обработки.
- **Markdown отчет** для человеко-читаемого формата.
- **Кластеризованные отчеты** для детального анализа.

##### Кластеризация данных
**Метод `get_clustered_summaries()`** группирует данные по параметрам:

- Устройство (Desktop/Mobile)
- Троттлинг (3G/4G/No_throttling)
- Геолокация (Moscow/Novosibirsk)
- Браузер (chromium/firefox/webkit)

##### Сравнительный анализ
**Метод `create_cluster_comparison_report()`** предоставляет:

- Сводную таблицу по кластерам.
- Детальное сравнение ключевых метрик.
- Статистический анализ аномалий.
- Рекомендации по оптимизации.

#### Форматы отчетов
Markdown отчет включает:

1. **Общая статистика**
    
    - Количество запусков.
    - Процент проблемных и упавших тестов.
    - Список протестированных URL.
        
2. **Анализ ошибок**
    
    - Критические ошибки с частотами.    
    - Примеры проблемных URL.
        
3. **Производительность по шагам**
    
    - PPI для каждого шага.
    - Ключевые метрики (LCP, FCP, CLS, etc.)
        
4. **Распределение параметров**
    
    - Статистика по устройствам, браузерам, геолокациям.
        
5. **Детализация метрик**
    
    - Оценка каждой метрики (✅🟢🟡🔴)
    - Сравнение с пороговыми значениями.

JSON отчет содержит:
```json
{
  "test_name": "test_smarttv_film_to_payment",
  "domain": "example.com",
  "total_runs": 50,
  "problematic_runs": 5,
  "failed_runs": 2,
  "steps": {
    "main_page": {
      "metrics": {
        "lcp": {"mean": 1200, "median": 1150, "min": 800, "max": 2000},
        "fcp": {"mean": 800, "median": 750, "min": 500, "max": 1200}
      },
      "booleans": {
        "popupAvailable": {"true_percentage": 95.0, "true_count": 48, "total": 50}
      },
      "ppi_stats": {"mean": 85.5, "median": 86.0, "stdev": 2.1}
    }
  }
}
```

#### Аналитические возможности
1. **Выявление проблем** - Автоматическое обнаружение проблемных метрик на основе пороговых значений из конфигурации.
2. **Статистический анализ**
- Аномалии: отклонения > 2σ от среднего.
- Рекомендации: выявление лучших/худших конфигураций.
- Тренды: сравнение производительности между кластерами.
1. **Визуализация в отчетах**
- Иконки статуса: ✅ 🟢 🟡 🔴 для быстрой оценки
- Таблицы сравнения: компактное представление данных
- Градуированные оценки: "отлично", "хорошо", "удовлетворительно", "плохо"

## utils/collect_film_urls.py
Скрипт для сбора всех URL страниц фильмов на выбранном домене. Вызов основной функции:
```python
urls = collect_film_urls(
        base_url="https://tests.goodmovie.net",
        limit=100,          # максимум, который сайт принимает
        max_pages=200       # ~20 000 страниц
    )
```
Поддерживает пагинацию через ?offset=...&limit=...
Сохраняет уникальные URL в films.json и films.txt в папке data.

## utils/lighthouse_runner.py
Модуль управляющий интеграцией с Lighthouse.

`run_lighthouse_for_url`
Запускает Lighthouse CLI через subprocess для указанного url и возвращает JSON-отчёт.

`extract_metrics_from_lighthouse`
Извлекает числовые метрики из Lighthouse-отчёта.

## utils/log_issues.py
Модуль для удобного логирования возникающих во время тестирования ошибок.
Анализирует report и дописывает проблемные метрики в лог-файл. Вызывать внутри теста после заполнения report.

## utils/metrics.py
Модуль для сбора метрик (частично неактуальный).

## utils/scenario_detector.py
Модуль для распознавания сценария проигрывания видео на странице фильма.

## utils/report_explainer.py
Модуль для составления индивидуального отчета по запуску одиночного теста.
Не актуален.

## test/shared/base_user_flow_test.py
### BaseUserFlowTest
`BaseUserFlowTest` - это базовый класс, реализующий шаблонный метод для тестирования пользовательских сценариев. Поддерживает кастомные шаги, обрабатывает ошибки, интегрирован с отчетом в allure.
#### Структура отчета
```python
report = {
    "test_name": "test_example",
    "domain": "calls7",
    "film_url": "https://...",
    "device": "Desktop",
    "throttling": "No_throttling", 
    "geoposition": "Moscow",
    "browser_type": "chromium",
    "steps": {
        "film_page": {"playerInitTime": 1200, "videoStartTime": 1500},
        "pay_page": {"iframeCpLoadTime": 2000},
        "after_payment_popup": {...},
        "after_return_without_payment": {...}
    },
    "is_problematic_flow": False,
    "error": None
}
```

#### Методы
#### `_goto_main_page(page, request, report)`
**Назначение**: Первоначальная загрузка главной страницы домена

#### `_goto_film_page_and_init_player(page, film_url)`
**Назначение**: Загрузка страницы фильма и инициализация видеоплеера
**Возвращаемые метрики**:
- `playerInitTime` - время инициализации плеера (мс)
- `videoStartTime` - время готовности видео к воспроизведению (мс)

#### `_wait_for_player_simple(page, timeout=30)`
**Назначение**: Мониторинг готовности видеоплеера через JavaScript

#### `_start_video_and_collect_metrics(page, scenario)`
**Назначение**: Запуск воспроизведения видео и сбор метрик.
- Собирает метрики первого кадра через `metrics.inject_plyr_playing_listener`

#### `_collect_buffering_metrics(page)`
**Назначение**: Сбор статистики буферизации видео
**Метрики**:
- `rebufferCount` - количество случаев перебуферизации
- `rebufferDuration` - общая длительность буферизации (мс)

#### `_wait_for_popup_and_click(page, request, report)`
**Назначение**: Ожидание и взаимодействие с попапом оплаты.
**Метрики**:
- `popupAppearTime` - Время появления попапа
- `popupAvailable` - Доступность попапа
- `popupClickSuccess` - Успешность клика

#### `_collect_payment_metrics(page, iframe_start_time, request, report)`
**Назначение**: Сбор метрик загрузки страницы оплаты
**Метрики**:
- `iframeCpLoadTime` - время загрузки iframe с формой оплаты

#### `_check_payment_button_click(page, iframe, pay_method)`
**Назначение**: Взаимодействие с кнопками оплаты в зависимости от метода
**Поддерживаемые методы оплаты**:
- `card` - банковская карта
- `sbp` - Система быстрых платежей
- `tpay` - Другие платежные системы

#### `_wait_for_payment_form(page, iframe, pay_method)`
**Назначение**: Валидация появления формы оплаты после клика
**Проверяемые формы**:
- Для `card`: форма ввода банковской карты
- Для `sbp`: форма СБП

#### `_load_popup_after_closing_pay_form(page, iframe)`
**Назначение**: Тестирование сценария "закрыть и повторить"

#### `_retry_payment_from_vidu_popup(page)`
**Назначение**: Повторная попытка оплаты из попапа Vidu
**Метрики**:
- `loadTime` - время загрузки новой формы
- `success` - успешность операции

#### `_collect_lighthouse_metrics(url, request, report)`
**Назначение**: Сбор комплексных метрик производительности через Lighthouse
**Собираемые метрики**:
- **LCP** (Largest Contentful Paint) - загрузка самого большого контента
- **CLS** (Cumulative Layout Shift) - суммарный сдвиг макета
- **TBT** (Total Blocking Time) - общее время блокировки
- **TTFB** (Time to First Byte) - время до первого байта
- **INP** (Interaction to Next Paint) - время отклика на взаимодействие

## test/domains/goodmovie/test_user_flow.py
### TestGoodmovieUserFlow
Дочерний класс BaseUserFlowTest. Поддерживает одиночные и параметризованные запуски тестов при помощи использования @pytest.mark (маркеры указаны в pyproject.toml). 